import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./src/**/*.{js,ts,jsx,tsx,mdx}"],
  theme: {
    extend: {
      colors: {
        pfn: {
          night:  "#01161E",
          teal:   "#3C8689",
          cyan:   "#0088A2",
          mint:   "#A9DBA8",
          ivory:  "#ECD0A0",
          white:  "#FFFFFF",
        },
      },
      fontFamily: {
        brand: ["'Spectral SC'", "serif"],   // logo / brand
        display: ["'Bebas Neue'", "sans-serif"], // titoli
        sans: ["'Bebas Neue'", "system-ui", "sans-serif"], // testo UI
      },
      boxShadow: {
        soft: "0 6px 20px rgba(0,0,0,0.2)",
      },
      borderRadius: {
        xl2: "1.2rem",
      },
    },
  },
  plugins: [],
};

export default config;
